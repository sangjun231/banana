# ============================================================
# Terraform 변수 설정 - ECS 버전
# ============================================================
# 
# 사용 방법:
#   1. 이 파일을 복사: cp terraform.tfvars.example terraform.tfvars
#   2. terraform.tfvars에서 값 수정
#   3. terraform.tfvars는 .gitignore에 추가
# ============================================================


# ============================================================
# 공통 설정
# ============================================================

aws_region   = "ap-northeast-2"  # 서울 리전
environment  = "dev"             # dev, staging, prod
project_name = "websocket-chat"  # 리소스 이름 prefix


# ============================================================
# ECS 설정
# ============================================================

# ECS 태스크 CPU (256 = 0.25 vCPU, 512 = 0.5 vCPU, 1024 = 1 vCPU)
ecs_task_cpu = 256

# ECS 태스크 메모리 (MB)
# CPU 256: 512, 1024, 2048
# CPU 512: 1024, 2048, 3072, 4096
ecs_task_memory = 512

# 실행할 태스크 개수 (개발: 1, 프로덕션: 2+)
ecs_desired_count = 1


# ============================================================
# 비용 관리
# ============================================================

budget_alert_email   = "your-email@example.com"
monthly_budget_limit = 100  # ECS는 비용이 발생하므로 높게 설정


# ============================================================
# VPC 설정 (선택사항 - 기본값 사용 권장)
# ============================================================

# vpc_cidr           = "10.0.0.0/16"
# availability_zones = ["ap-northeast-2a", "ap-northeast-2c"]
